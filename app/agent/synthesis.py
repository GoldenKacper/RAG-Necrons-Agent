"""
Etap 3 (jeszcze pusty — Etap 0 to tylko struktura).

Tu powstanie:
  - synthesize_answer(question, chunks: list[SearchResult]) -> str
      osobne, CZYSTE wywolanie LLM (bez historii petli):
      sortowanie chunkow po score, opcjonalny limit tokenow,
      reuse format_context + konwencja [SOURCE n] + lista zrodel na koncu.
"""
